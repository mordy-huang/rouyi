# 云服务器部署指南

---

## 第一步：选购服务器

以阿里云为例，其他云厂商（腾讯云、华为云）流程类似。

### 推荐配置

| 用途 | 配置 | 预估价格 |
|------|------|---------|
| 学习/个人项目 | 2核 4G 轻量应用服务器 | ~70元/月 |
| 业务项目 | 2核 4G ECS 服务器 | ~100元/月 |
| 高并发项目 | 4核 8G ECS 服务器 | ~200元/月 |

### 购买流程

1. 进入 [阿里云官网](https://www.aliyun.com)
2. 搜索 **"轻量应用服务器"**（入门首选）
3. 选择：
   - **地域**：选离你最近的（华南-广州 或 华东-上海）
   - **镜像**：**Ubuntu 22.04**
   - **配置**：2核 4G 起
4. 付款后，在控制台找到服务器

> 提示：学生有优惠（"云翼计划"），首年约 100 元左右。

---

## 第二步：配置安全组（放行端口）

在阿里云控制台找到 **安全组** → **配置规则** → **入方向**，添加：

| 端口 | 用途 | 是否必须 |
|------|------|---------|
| 22 | SSH 连接 | ✅ 必须 |
| 80 | HTTP 前端页面 | ✅ 必须 |
| 443 | HTTPS（如有域名） | 按需 |
| 8080 | 后端 API（调试用） | 按需 |
| 3307 | 数据库外部访问 | ❌ 不建议开放 |
| 6380 | Redis 外部访问 | ❌ 不建议开放 |

> **安全建议**：3307 和 6380 不要开放到公网，只在服务器内部使用。

---

## 第三步：连接服务器

### Windows 使用 Terminal 或 PowerShell

```bash
ssh root@你的服务器IP
# 输入密码（购买时设置或重置的密码）
```

首次登录要求改密码，按提示操作即可。

> 如果忘记密码，在控制台 **重置实例密码**，重启后生效。

---

## 第四步：安装 Docker

连接上服务器后，一条一条执行：

### 4.1 更新包管理器

```bash
apt update && apt upgrade -y
```

### 4.2 安装 Docker（官方一键脚本）

```bash
curl -fsSL https://get.docker.com | bash -s docker
```

### 4.3 安装 Docker Compose

```bash
apt install docker-compose-plugin -y
```

### 4.4 验证

```bash
docker --version
docker compose version
```

---

## 第五步：配置 Docker 镜像加速

国内拉取镜像慢，配好加速器：

```bash
# 创建配置目录
mkdir -p /etc/docker

# 写入镜像加速配置
cat > /etc/docker/daemon.json << 'EOF'
{
  "registry-mirrors": [
    "https://docker.1ms.run",
    "https://docker.xuanyuan.me",
    "https://docker.m.daocloud.io"
  ]
}
EOF

# 重启 Docker
systemctl daemon-reload
systemctl restart docker
```

---

## 第六步：部署项目

### 6.1 上传代码

**方式一：直接从 Git 拉取（推荐）**

如果你把项目推到了 GitHub / Gitee：

```bash
# 安装 git
apt install git -y

# 拉取代码
git clone https://github.com/你的用户名/你的仓库.git ruoyi-vue

cd ruoyi-vue
```

**方式二：本地打包上传**

在你本机的项目目录打开终端：

```bash
# 本地打包所有 Docker 镜像并导出
docker compose build
docker save ruoyi-admin:latest -o ruoyi-admin.tar
docker save ruoyi-ui:latest -o ruoyi-ui.tar
```

然后把 tar 文件上传到服务器。

### 6.2 修改配置（如有需要）

```bash
# 修改文件上传路径（Windows → Linux）
vi ruoyi-admin/src/main/resources/application.yml
# 把 profile: D:/ruoyi/uploadPath 改为 profile: /home/ruoyi/uploadPath
```

### 6.3 启动

```bash
docker compose up -d
```

### 6.4 查看状态

```bash
docker compose ps
docker compose logs ruoyi-admin  # 看后端日志
```

---

## 第七步：访问

打开浏览器，访问：

```
http://你的服务器IP
```

默认账号：`admin` / `admin123`

---

## 常用运维命令速查

```bash
# SSH 登录
ssh root@你的服务器IP

# 进入项目目录
cd ruoyi-vue

# 更新代码
git pull

# 重新构建并启动
docker compose up -d --build

# 查看日志
docker compose logs -f

# 查看日志（只查后端的）
docker compose logs -f ruoyi-admin

# 重启单个服务
docker compose restart ruoyi-admin

# 停止所有服务
docker compose down

# 进入容器内部
docker exec -it ry-admin bash
```

---

## 附：没有域名怎么办？

直接用 IP 访问就行。但如果想配 HTTPS，需要：

1. 买个域名（阿里云万网，约 30-50 元/年）
2. DNS 解析到服务器 IP
3. 用 certbot 申请免费 SSL 证书
4. 修改 nginx 配置

等你要配的时候我再来帮你也行。
